<div class="ui orange three inverted attached menu navbar-adjust">
  <div class="ui container ">
    <%= link_to "Chatroom", root_path, class: "item" %>
    <div class="right menu">
      <div class="ui dropdown item">
        Account <i class="dropdown icon"></i>
        <div class="menu">
          <% if !logged_in? %>
            <%= link_to "Login", login_path, class: "item" %>
            <%= link_to "Sign Up", signup_path, class: "item" %>
          <% else %>
            <%= link_to "Logout", logout_path, method: :delete, class: "item" %>
          <% end %>
        </div>
      </div>
      <div class="item">
          <% if !logged_in? %>
            <%= link_to "Login", login_path, class: "ui secondary button" %>
          <% else %>
            <%= link_to "Logout", logout_path, method: :delete, class: "ui secondary button" %>
          <% end %>
      </div>
    </div>
  </div>
</div>

